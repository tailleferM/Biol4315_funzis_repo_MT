---
title: "Biol4315 Lab-1 Report"
output: html_document
---

# I forked Ilay's Repo

```{r c1, eval=FALSE}
install.packages('vegan')
```

Loading ggplot2 and plotting speed vs dist from cars dataset

```{r c2}
library(ggplot2)
ggplot(cars,aes(x = speed, y = dist))+
  geom_point()+
  labs(title = "Car speed vs Distance", x = 'Speed', y = 'Distance')
```
  
```{r c3, eval=FALSE}
docker pull nextgenusfs/funannotate
```

```{r c4, eval=FALSE}
conda create -n sequali-env python = 3.10 -c conda-forge
conda activate sequali-env
conda install sequali -c conda-forge -c bioconda
```
